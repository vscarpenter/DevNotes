/**
 * Search module exports
 */

export { SearchEngine, searchEngine } from './SearchEngine';
export type { 
  SearchResult, 
  SearchHighlight, 
  SearchIndex, 
  SearchOptions, 
  SearchFilters 
} from '../../types/search';